# yaml-language-server: $schema=https://hupe1980.github.io/agentplane/agent.schema.json
# §20 EnWG Diskriminierungsfreiheit parity monitor and BNetzA KPI reporting. Tracks STP rates across market roles, generates BNetzA Diskriminierungsbericht, and flags parity violations.
#
# Every grant below is a tool this specialist's own procedure names, and every
# tool the procedure names is granted. `cargo xtask check-prompt-tools` holds the
# two together in both directions, so a grant nobody instructs — a whole server's
# read surface handed over because the specialist touches that server — cannot
# come back. `mutates` is derived from the server's own `read_only_hint`, and
# there are no mutating grants here at all: a `tool-calling` agent cannot
# dispatch one, so declaring it would read as an ability this agent does not
# have. See the note above `budgets`.
# Cross-cutting: present in every role-scoped build.
apiVersion: agentplane.hupe1980.github.io/v1alpha1
kind: Agent
metadata:
  name: compliance-agent
  version: "2.0.0"
spec:
  execution:
    kind: tool-calling
    max_turns: 12

  identity:
    role: >-
      §20 EnWG Diskriminierungsfreiheit parity monitor and BNetzA KPI reporting.
      Tracks STP rates across market roles, generates BNetzA
      Diskriminierungsbericht, and flags parity violations.
    constraints: |
      You are the §20 EnWG compliance and BNetzA reporting specialist.

      ## TRIGGERED BY
      - Manual or scheduled compliance checks
      - `de.obs.stp.parity.alert`

      ## PROCEDURE

      1. Call obsd `get_parity_report` for Strom and Gas process families.
      2. Check: NB STP rate for LF ≈ NB STP rate for own group (§20 Abs. 1 EnWG).
      3. Call obsd `get_kpi_report` for current KPI summary, and obsd `get_stp_rate` for
         a family the parity report shows out of line. Together these are the BNetzA
         Diskriminierungsbericht inputs; no single tool produces that report.
      4. Call processd `list_affiliate_decisions` — the same question decision by
         decision, split by whether the counterparty is affiliated with this operator.
         A parity gap the aggregate hides shows up here.
      5. For a role whose rate is low, call processd `get_stp_breakdown_by_erc`: a
         deviation with no dominant refusal code behind it is a reporting artefact, and
         one with a single dominant code is a data gap somebody can close.
      6. Identify any parity deviation above obsd's screening threshold of
         **5 percentage points** between market roles. The threshold is mako's
         own, not a published one: § 20 Abs. 1 EnWG obliges discriminationsfreien
         Netzzugang and names no figure, so a deviation is evidence that needs
         explaining rather than a breach by arithmetic.
      7. Generate structured compliance summary.

  # What this agent is in a multi-agent arrangement.
  #
  # MAST measures inter-agent misalignment at 36.9 % of observed multi-agent
  # failures — a class that exists only if you choose it. Declaring the shape is
  # how you choose not to: `single` means one agent, one context, many tools, and
  # the inter-agent failure surface is structurally absent. `specialist` means it
  # hands off to nobody, which is what `max_delegation_depth: 0` already enforces
  # — stated here so the two cannot disagree.
  topology:
    mode: single
    role: specialist

  capabilities:
    provides: [compliance]

  models:
    privileged:  { provider: anthropic, model: claude-sonnet-5 }
    # Reads the agent's own answer during memory formation — model output, and
    # therefore untrusted. A model that cannot call a tool is what should be
    # looking at it.
    quarantined: { provider: anthropic, model: claude-haiku-4-5 }

  # Memory: only where the subject is the operator itself.
  #
  # `subject` is a **literal** in this file, and `Recall` is keyed by it — there
  # is no automatic per-case narrowing. A per-customer specialist declaring a
  # fixed subject would therefore pool every customer's facts under one key and
  # surface one customer's history inside another's run. That is not a feature
  # with a caveat; it is a privacy defect, and it is why the twenty-six
  # specialists that reason about a Marktlokation have **no** memory block. The
  # absence is a decision, recorded here so it does not read as an oversight.
  #
  # This subject is the operator's own regulatory posture. It is not customer
  # data, it is the same for every run, and a trend across runs is exactly what
  # it is for.
  #
  # Formation reads the agent's own answer, which is model output and therefore
  # untrusted — so it runs on the **quarantined** model. That is the one place
  # besides a plan's `parse` steps where the declarative tier points a model at
  # untrusted-derived content, and it is what makes the second model role mean
  # something rather than decorate the file.
  # 0.19 moved formation under `memory`, beside a `recall` channel this
  # plane does not use: recall order is a ranking signal any writer can
  # influence, and adopting it is a decision somebody makes deliberately.
  memory:
    formation:
      subject: "operator-parity-posture"
      purpose: >-
        Track how this operator's own Gleichbehandlung and STP posture moves between audits, so a slow drift is visible as a trend rather than re-derived from scratch every run.
      instruction: >-
        Record at most three durable facts about the OPERATOR's own regulatory posture: the parity deviation observed, the process families it appeared in, and any BNetzA-reportable finding. Record nothing about an individual customer, Marktlokation or counterparty.
      max_items: 3
      # Two reporting years. Long enough for a year-on-year comparison, short
      # enough that a posture nobody has revisited expires instead of hardening
      # into a standing instruction.
      retention_seconds: 63113904
      max_sensitivity: internal

  security:
    max_sensitivity_egress: internal
    max_delegation_depth: 0
    max_sensitivity_journaled: internal

  # No mutating tool grant, and no approval: this specialist reads and reports.
  # Both absences are the same fact — a `tool-calling` agent cannot dispatch a
  # mutating call at all, because the arguments come from a model completion,
  # which agentplane labels untrusted by construction, and its taint gate refuses
  # a mutating sink with untrusted arguments. Declaring the grant anyway would
  # read as an ability this agent does not have.
  #
  # It does declare `oversight`, for **triage** — a row beside a finished answer
  # rather than a gate in front of a call. That is the only mode that fits an
  # agent which cannot act: gating its answer would gate nothing while suspending
  # a run per finding.
  #

  # Human-in-the-loop, beside the answer rather than in front of it.
  #
  # `approval: none` — this specialist cannot act, so there is nothing to gate.
  # A `tool-calling` agent's arguments come from a model completion, which the
  # taint gate refuses at a mutating sink; it reads and reports. `tools-only`
  # would therefore gate nothing, and `required` would suspend a run per finding
  # and cost a case row until somebody clears it.
  #
  # `triage` is the third mode agentplane 0.15 added for exactly this: the run
  # completes and returns, and a matching answer *also* opens a worklist row.
  # Reporting, not control flow — which is why a triage rule may carry a
  # predicate where `approval` deliberately may not.
  #
  # Every `path` below is typed against this agent's own `output.schema` at parse
  # time, so a rule that could never fire is refused rather than reading in
  # review exactly like one that does. That check only became possible once the
  # answers were schemas instead of prose.
  oversight:
    approval: none
    # `approval: none` means nothing waits, so this bounds no wait — the field
    # is required and states the same window the rule below uses. One figure
    # written twice is cheaper than one field meaning two things.
    deadline:
      name: parity-breach-review
      kind: working-days
      params: { n: 2 }
    # A triage row *is* the finding, so an unanswered one widens rather than
    # closes: `deny` would take it out of the worklist and delete the delivery
    # of a breach that was correctly detected. (gabi-gas-agent's dispatching
    # approval stays `deny` for the opposite reason — it gates, so it must fail
    # closed.)
    on_expiry: escalate
    escalate_to: [mako-operations]
    triage:
      - name: parity-breach
        audience: [regulatory]
        priority: urgent
        summary: >-
          §20 Abs. 1 EnWG parity deviation between market roles. BNetzA-reportable — confirm before the next Diskriminierungsbericht.
        when:
          - path: /parity_status
            equals: "VIOLATION"
        deadline:
          name: parity-breach-review
          kind: working-days
          params: { n: 2 }

  budgets:
    max_tokens: 200000
    max_steps: 36
    max_effects: 60
    max_wallclock_secs: 900

  # The result contract. It was a prose fence inside the prompt, which made
  # every consumer a parser of free text; as a schema the model is held to it
  # and a change to it changes the manifest digest.
  output:
    schema:
      type: object
      additionalProperties: false
      required:
        - parity_status
        - stp_rate_overall_pct
        - parity_deviation_pct
      properties:
        parity_status:
          type: string
          enum:
            - COMPLIANT
            - AT_RISK
            - VIOLATION
        stp_rate_overall_pct:
          type: number
        parity_deviation_pct:
          type: number
        bnetza_reportable:
          type: string
          enum:
            - YES
            - NO
        issues:
          type: array
          items:
            type: string

  # Knowledge, granted rather than copied.
  #
  # mako's MCP servers publish 57 step-by-step prompts across 15 servers for
  # their own procedures. A specialist names the one it needs; a hand-typed
  # paraphrase in `constraints` would be a second copy with nothing keeping the
  # two in agreement — the server's prompt changes with the code, a paraphrase
  # changes when somebody remembers.
  #
  # A context grant is not a tool grant. Reading a prompt authorises no action,
  # but it does cross a trust and data-egress boundary, which is why it is
  # declared here where a reviewer sees it rather than fetched silently.
  context:
    prompts:
      - server: obsd
        name: audit-kpi
        max_input_sensitivity: internal
        output_sensitivity: internal

  tools:
    - ref: tool://obsd/get_parity_report
      mutates: false
      max_sensitivity: internal
      description: >-
        STP rates by market role — the § 20 Abs. 1 EnWG parity comparison
        itself.

    - ref: tool://obsd/get_kpi_report
      mutates: false
      max_sensitivity: internal
      description: >-
        The KPI summary that forms the rest of the Diskriminierungsbericht.

    - ref: tool://obsd/get_stp_rate
      mutates: false
      max_sensitivity: internal
      description: >-
        One family's straight-through rate, to explain a deviation the parity
        report shows.

    - ref: tool://processd/list_affiliate_decisions
      mutates: false
      max_sensitivity: internal
      description: >-
        Decisions split by affiliated and non-affiliated counterparty — the
        discrimination question, per decision.

    - ref: tool://processd/get_stp_breakdown_by_erc
      mutates: false
      max_sensitivity: internal
      description: >-
        Which refusal codes drive a role's lower rate, so a deviation has a
        cause.
