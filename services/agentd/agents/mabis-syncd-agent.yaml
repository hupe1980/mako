# yaml-language-server: $schema=https://hupe1980.github.io/agentplane/agent.schema.json
# MaBiS Summenzeitreihe submission monitor. Tracks the Werktag-based Erstaufschlag/Clearing windows (BK6-24-174 Anlage 3 §3.10), detects failed aggregations, and diagnoses BIKO delivery issues.
#
# Generated from the compiled specialist definition during the agentplane
# cutover, and narrowed twice since. `mutates` is derived from the MCP server's
# own `read_only_hint` — the cutover inherited it from servers that declared
# nothing, so 91 plain reads were marked mutating and would have stopped for an
# approval on every call. The mutating grants are gone entirely, because a
# `tool-calling` agent cannot dispatch one: see the note above `budgets`.
#
# Narrowing the read set further per specialist is domain review, not mechanical
# work.
# Role: NB. Compiled only into a `role-nb` build (and the default all-roles build).
apiVersion: agentplane.hupe1980.github.io/v1alpha1
kind: Agent
metadata:
  name: mabis-syncd-agent
  version: "2.1.0"
spec:
  execution:
    kind: tool-calling
    max_turns: 10

  identity:
    role: >-
      MaBiS Summenzeitreihe submission monitor. Tracks the Werktag-based
      Erstaufschlag/Clearing windows (BK6-24-174 Anlage 3 §3.10), detects failed
      aggregations, and diagnoses BIKO delivery issues.
    constraints: |
      You are the MaBiS Summenzeitreihe submission specialist.

      ## TRIGGERED BY
      - `de.mabis.submission.failed` — an aggregation or BIKO submission failed; the payload carries run_id, phase and attempt_count
      - `de.mabis.korrekturbedarf.opened` — a negative Prüfmitteilung opened a §9.8.1 correction obligation
      - `de.messwert.reading.quality.warning` — quality issue may affect Summenzeitreihe accuracy

      ## MABIS WINDOWS (BK6-24-174 Anlage 3 §3.10, Werktage after month end)
      - Erstaufschlag (BKA): ≤ 10 Werktage — a new version becomes Abrechnungsdaten directly
      - Clearing (BKA): ≤ 30 Werktage — a new version starts as Prüfdaten and needs a positive Prüfmitteilung
      - After 30 Werktage: KBKA (korrigierte Bilanzkreisabrechnung)
      - mabis-syncd submits at 05:00 UTC on the configured Erstaufschlag-Werktag (default 10)

      ## ON SUBMISSION FAILED

      1. Call mabis-syncd `get_submission_run` with the event's run_id for the full record.
      2. Read `attempt_count`: below 3 the scheduler retries by itself — report PENDING, do not escalate.
      3. At 3, triage `error_msg`: an aggregation error means edmd data is missing
         (call edmd `get_billing_period` and `get_quality_warnings` to say which MaLos);
         a submission error means the MSCONS never reached the BIKO (makod dispatch).
      4. State which window is closing: within Erstaufschlag a retry still lands as
         Abrechnungsdaten; after it, only as Prüfdaten.

      ## ON KORREKTURBEDARF OPENED

      1. Call mabis-syncd `list_korrekturbedarf` for every open obligation.
      2. For the affected period, call mabis-syncd `get_submission_status` to see
         whether a correcting run is already under way.
      3. The correction itself is an operator action (POST /api/v1/sync with
         corrects_run_id) — name the run, the Bilanzierungsgebiet and the Clearing
         deadline in your answer.

      ## ON QUALITY WARNING

      1. Call mabis-syncd `get_submission_status` — a warning matters only if a
         submission for the affected period is pending or recent.
      2. Call edmd `get_summenzeitreihe` to check whether the affected MaLo's
         intervals are substituted in the aggregate.

  # What this agent is in a multi-agent arrangement.
  #
  # MAST measures inter-agent misalignment at 36.9 % of observed multi-agent
  # failures — a class that exists only if you choose it. Declaring the shape is
  # how you choose not to: `single` means one agent, one context, many tools, and
  # the inter-agent failure surface is structurally absent. `specialist` means it
  # hands off to nobody, which is what `max_delegation_depth: 0` already enforces
  # — stated here so the two cannot disagree.
  topology:
    mode: single
    role: specialist

  capabilities:
    provides: [mabis.syncd]

  models:
    privileged:  { provider: anthropic, model: claude-sonnet-5 }

  security:
    max_sensitivity_egress: internal
    max_delegation_depth: 0
    max_sensitivity_journaled: internal

  # No `oversight` block, and no mutating tool grant: this specialist reads and
  # reports. Both absences are the same fact — a `tool-calling` agent cannot
  # dispatch a mutating call at all, because the arguments come from a model
  # completion, which agentplane labels untrusted by construction, and its taint
  # gate refuses a mutating sink with untrusted arguments. Declaring the grant
  # anyway would read as an ability this agent does not have.
  #
  # Acting on a conclusion is a `planned` specialist's job — see
  # `gabi-gas-agent`, whose plan passes `$input/…` references the runtime
  # resolves itself, so the authority-bearing arguments keep the run input's own
  # labels and a named human approves the call before it leaves.

  # Human-in-the-loop, beside the answer rather than in front of it.
  #
  # `approval: none` — this specialist cannot act, so there is nothing to gate.
  # A `tool-calling` agent's arguments come from a model completion, which the
  # taint gate refuses at a mutating sink; it reads and reports. `tools-only`
  # would therefore gate nothing, and `required` would suspend a run per finding
  # and cost a case row until somebody clears it.
  #
  # `triage` is the third mode agentplane 0.15 added for exactly this: the run
  # completes and returns, and a matching answer *also* opens a worklist row.
  # Reporting, not control flow — which is why a triage rule may carry a
  # predicate where `approval` deliberately may not.
  #
  # Every `path` below is typed against this agent's own `output.schema` at parse
  # time, so a rule that could never fire is refused rather than reading in
  # review exactly like one that does. That check only became possible once the
  # answers were schemas instead of prose.
  oversight:
    approval: none
    # `approval: none` means nothing waits, so this bounds no wait — the field
    # is required and states the same window the rule below uses. One figure
    # written twice is cheaper than one field meaning two things.
    deadline:
      name: mabis-submission-review
      kind: working-days
      params: { n: 1 }
    # A triage row *is* the finding, so an unanswered one widens rather than
    # closes: `deny` would take it out of the worklist and delete the delivery
    # of a breach that was correctly detected. (gabi-gas-agent's dispatching
    # approval stays `deny` for the opposite reason — it gates, so it must fail
    # closed.)
    on_expiry: escalate
    escalate_to: [mako-operations]
    triage:
      - name: mabis-submission
        audience: [netzbilanz]
        priority: urgent
        summary: >-
          A MaBiS submission failed or missed its window (BK6-24-174 Anlage 3 §3.10). The window is fixed Werktage after month end.
        when:
          - path: /submission_status
            in: ["FAILED", "OVERDUE"]
        deadline:
          name: mabis-submission-review
          kind: working-days
          params: { n: 1 }

  budgets:
    max_tokens: 200000
    max_steps: 30
    max_effects: 50
    max_wallclock_secs: 900

  # The result contract. It was a prose fence inside the prompt, which made
  # every consumer a parser of free text; as a schema the model is held to it
  # and a change to it changes the manifest digest.
  output:
    schema:
      type: object
      additionalProperties: false
      required:
        - submission_status
        - submission_type
        - deadline_utc
      properties:
        submission_status:
          type: string
          enum:
            - ON_TIME
            - OVERDUE
            - FAILED
            - PENDING
        submission_type:
          type: string
          enum:
            - VORLAEUFIG
            - ENDGUELTIG
        deadline_utc:
          type: string
        failed_malo_count:
          type: number
        action:
          type: string
          enum:
            - NONE
            - RETRY
            - ESCALATE

  # Knowledge, granted rather than copied.
  #
  # Every mako MCP server publishes step-by-step prompts for its own procedures —
  # fifty of them across thirteen servers — and not one manifest reached a single
  # one. Each specialist instead carried a hand-typed paraphrase in its
  # `constraints`, which is a second copy of a procedure with nothing keeping the
  # two in agreement: the server's prompt changes with the code, the paraphrase
  # changes when somebody remembers.
  #
  # A context grant is not a tool grant. Reading a prompt authorises no action,
  # but it does cross a trust and data-egress boundary, which is why it is
  # declared here where a reviewer sees it rather than fetched silently.
  context:
    prompts:
      - server: mabis_syncd
        name: submission-triage
        max_input_sensitivity: internal
        output_sensitivity: internal

  tools:
    - ref: tool://mabis_syncd/get_submission_status
      mutates: false
      max_sensitivity: internal
      description: "recent submission runs + failed / retry-exhausted / Korrekturbedarf counts"
    - ref: tool://mabis_syncd/list_failed_submissions
      mutates: false
      max_sensitivity: internal
      description: "failed Summenzeitreihe runs with attempt_count and error_msg"
    - ref: tool://mabis_syncd/get_submission_run
      mutates: false
      max_sensitivity: internal
      description: "one submission run by UUID"
    - ref: tool://mabis_syncd/list_korrekturbedarf
      mutates: false
      max_sensitivity: internal
      description: "open negative Prüfmitteilungen (§9.8.1 obligations)"
    - ref: tool://edmd/get_timeseries
      mutates: false
      max_sensitivity: internal
      description: "get timeseries via edmd"
    - ref: tool://edmd/get_imbalance
      mutates: false
      max_sensitivity: internal
      description: "get imbalance via edmd"
    - ref: tool://edmd/get_billing_period
      mutates: false
      max_sensitivity: internal
      description: "get billing period via edmd"
    - ref: tool://edmd/get_device_history
      mutates: false
      max_sensitivity: internal
      description: "get device history via edmd"
    - ref: tool://edmd/get_quality_warnings
      mutates: false
      max_sensitivity: internal
      description: "get quality warnings via edmd"
    - ref: tool://edmd/list_reading_orders
      mutates: false
      max_sensitivity: internal
      description: "list reading orders via edmd"
    - ref: tool://edmd/list_overdue_reading_orders
      mutates: false
      max_sensitivity: internal
      description: "list overdue reading orders via edmd"
    - ref: tool://edmd/get_correction_history
      mutates: false
      max_sensitivity: internal
      description: "get correction history via edmd"
    - ref: tool://edmd/validate_timeseries
      mutates: false
      max_sensitivity: internal
      description: "validate timeseries via edmd"
    - ref: tool://edmd/get_quality_assessments
      mutates: false
      max_sensitivity: internal
      description: "get quality assessments via edmd"
    - ref: tool://edmd/get_summenzeitreihe
      mutates: false
      max_sensitivity: internal
      description: "get summenzeitreihe via edmd"
    - ref: tool://edmd/get_annual_forecast
      mutates: false
      max_sensitivity: internal
      description: "get annual forecast via edmd"
    - ref: tool://edmd/get_gas_quality
      mutates: false
      max_sensitivity: internal
      description: "get gas quality via edmd"
    - ref: tool://obsd/get_process
      mutates: false
      max_sensitivity: internal
      description: "get process via obsd"
    - ref: tool://obsd/list_overdue_processes
      mutates: false
      max_sensitivity: internal
      description: "list overdue processes via obsd"
    - ref: tool://obsd/get_kpi_report
      mutates: false
      max_sensitivity: internal
      description: "get kpi report via obsd"
    - ref: tool://obsd/get_parity_report
      mutates: false
      max_sensitivity: internal
      description: "get parity report via obsd"
    - ref: tool://obsd/get_stp_rate
      mutates: false
      max_sensitivity: internal
      description: "get stp rate via obsd"
    - ref: tool://obsd/list_processes_by_family
      mutates: false
      max_sensitivity: internal
      description: "list processes by family via obsd"
    - ref: tool://marktd/get_malo
      mutates: false
      max_sensitivity: confidential
      description: "get malo via marktd"
    - ref: tool://marktd/list_partners
      mutates: false
      max_sensitivity: confidential
      description: "list partners via marktd"
    - ref: tool://marktd/get_preisblatt
      mutates: false
      max_sensitivity: confidential
      description: "get preisblatt via marktd"
    - ref: tool://marktd/get_versorgungsstatus
      mutates: false
      max_sensitivity: confidential
      description: "get versorgungsstatus via marktd"
    - ref: tool://marktd/get_nb_contract
      mutates: false
      max_sensitivity: confidential
      description: "get nb contract via marktd"
    - ref: tool://marktd/get_correlation
      mutates: false
      max_sensitivity: confidential
      description: "get correlation via marktd"
    - ref: tool://marktd/list_pricat_versions
      mutates: false
      max_sensitivity: confidential
      description: "list pricat versions via marktd"
    - ref: tool://marktd/list_malo
      mutates: false
      max_sensitivity: confidential
      description: "list malo via marktd"
    - ref: tool://marktd/get_melo
      mutates: false
      max_sensitivity: confidential
      description: "get melo via marktd"
    - ref: tool://marktd/get_rollenzuordnung
      mutates: false
      max_sensitivity: confidential
      description: "get rollenzuordnung via marktd"
    - ref: tool://marktd/get_versorgungsstatus_history
      mutates: false
      max_sensitivity: confidential
      description: "get versorgungsstatus history via marktd"
    - ref: tool://marktd/get_nb_energiemix
      mutates: false
      max_sensitivity: confidential
      description: "get nb energiemix via marktd"
    - ref: tool://marktd/get_technische_ressource
      mutates: false
      max_sensitivity: confidential
      description: "get technische ressource via marktd"
    - ref: tool://marktd/get_steuerbare_ressource
      mutates: false
      max_sensitivity: confidential
      description: "get steuerbare ressource via marktd"
    - ref: tool://marktd/get_partner
      mutates: false
      max_sensitivity: confidential
      description: "get partner via marktd"
    - ref: tool://marktd/get_melo_standorteigenschaften
      mutates: false
      max_sensitivity: confidential
      description: "get melo standorteigenschaften via marktd"
    - ref: tool://marktd/get_versorgung_at
      mutates: false
      max_sensitivity: confidential
      description: "get versorgung at via marktd"
