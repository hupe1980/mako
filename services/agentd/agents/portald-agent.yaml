# yaml-language-server: $schema=https://hupe1980.github.io/agentplane/agent.schema.json
# Customer portal proactive notification orchestrator. Triggers invoice-ready notifications, EEG Förderung expiry alerts, Mahnung reminders, and Tarifwechsel confirmation messages.
#
# Generated from the compiled specialist definition during the agentplane
# cutover, and narrowed twice since. `mutates` is derived from the MCP server's
# own `read_only_hint` — the cutover inherited it from servers that declared
# nothing, so 91 plain reads were marked mutating and would have stopped for an
# approval on every call. The mutating grants are gone entirely, because a
# `tool-calling` agent cannot dispatch one: see the note above `budgets`.
#
# Narrowing the read set further per specialist is domain review, not mechanical
# work.
# Role: LF. Compiled only into a `role-lf` build (and the default all-roles build).
apiVersion: agentplane.hupe1980.github.io/v1alpha1
kind: Agent
metadata:
  name: portald-agent
  version: "1.1.0"
spec:
  execution:
    kind: tool-calling
    max_turns: 8

  identity:
    role: >-
      Customer portal proactive notification orchestrator. Triggers invoice-ready
      notifications, EEG Förderung expiry alerts, Mahnung reminders, and
      Tarifwechsel confirmation messages.
    constraints: |
      You are the customer portal notification specialist.

      ## TRIGGERED BY
      - `de.billing.rechnung.erstellt` — new invoice → notify customer
      - `de.eeg.anlage.foerderung-auslaufend` — EEG expiry → notify plant operator
      - `de.accounting.mahnung.issued` — Mahnung → dunning notification
      - `de.vertrag.*` — contract lifecycle → keep the portal view current

      ## PROCEDURE

      1. Determine notification type from event.
      2. Call portald to check if customer has active portal session.
      3. Retrieve relevant data (invoice brutto_eur, EEG foerderendedatum, Mahnstufe).
      4. Compose notification summary for customer-facing display.
      5. Flag if notification requires operator approval before sending.

  # What this agent is in a multi-agent arrangement.
  #
  # MAST measures inter-agent misalignment at 36.9 % of observed multi-agent
  # failures — a class that exists only if you choose it. Declaring the shape is
  # how you choose not to: `single` means one agent, one context, many tools, and
  # the inter-agent failure surface is structurally absent. `specialist` means it
  # hands off to nobody, which is what `max_delegation_depth: 0` already enforces
  # — stated here so the two cannot disagree.
  topology:
    mode: single
    role: specialist

  capabilities:
    provides: [portald]

  models:
    privileged:  { provider: anthropic, model: claude-sonnet-5 }

  security:
    max_sensitivity_egress: internal
    max_delegation_depth: 0
    max_sensitivity_journaled: internal

  # No `oversight` block, and no mutating tool grant: this specialist reads and
  # reports. Both absences are the same fact — a `tool-calling` agent cannot
  # dispatch a mutating call at all, because the arguments come from a model
  # completion, which agentplane labels untrusted by construction, and its taint
  # gate refuses a mutating sink with untrusted arguments. Declaring the grant
  # anyway would read as an ability this agent does not have.
  #
  # Acting on a conclusion is a `planned` specialist's job — see
  # `gabi-gas-agent`, whose plan passes `$input/…` references the runtime
  # resolves itself, so the authority-bearing arguments keep the run input's own
  # labels and a named human approves the call before it leaves.

  budgets:
    max_tokens: 200000
    max_steps: 24
    max_effects: 40
    max_wallclock_secs: 900

  # The result contract. It was a prose fence inside the prompt, which made
  # every consumer a parser of free text; as a schema the model is held to it
  # and a change to it changes the manifest digest.
  output:
    schema:
      type: object
      additionalProperties: false
      required:
        - notification_type
        - customer_portal_active
        - notification_safe
      properties:
        notification_type:
          type: string
          enum:
            - INVOICE_READY
            - EEG_EXPIRY
            - DUNNING
            - TARIFF_CHANGE
        customer_portal_active:
          type: string
          enum:
            - YES
            - NO
        notification_safe:
          type: string
          enum:
            - YES
            - NEEDS_REVIEW
        summary:
          type: string

  # Knowledge, granted rather than copied.
  #
  # Every mako MCP server publishes step-by-step prompts for its own procedures —
  # fifty of them across thirteen servers — and not one manifest reached a single
  # one. Each specialist instead carried a hand-typed paraphrase in its
  # `constraints`, which is a second copy of a procedure with nothing keeping the
  # two in agreement: the server's prompt changes with the code, the paraphrase
  # changes when somebody remembers.
  #
  # A context grant is not a tool grant. Reading a prompt authorises no action,
  # but it does cross a trust and data-egress boundary, which is why it is
  # declared here where a reviewer sees it rather than fetched silently.
  context:
    prompts:
      - server: portald
        name: customer-overview
        max_input_sensitivity: internal
        output_sensitivity: internal
      - server: portald
        name: billing-dispute
        max_input_sensitivity: internal
        output_sensitivity: internal

  tools:
    - ref: tool://portald/get_dashboard
      mutates: false
      max_sensitivity: confidential
      description: "get dashboard via portald"
    - ref: tool://portald/get_lastgang
      mutates: false
      max_sensitivity: confidential
      description: "get lastgang via portald"
    - ref: tool://portald/get_invoices
      mutates: false
      max_sensitivity: confidential
      description: "get invoices via portald"
    - ref: tool://portald/get_balance
      mutates: false
      max_sensitivity: confidential
      description: "get balance via portald"
    - ref: tool://portald/get_kontoauszug
      mutates: false
      max_sensitivity: confidential
      description: "get kontoauszug via portald"
    - ref: tool://portald/get_vorauszahlung
      mutates: false
      max_sensitivity: confidential
      description: "get vorauszahlung via portald"
    - ref: tool://portald/get_eeg_status
      mutates: false
      max_sensitivity: confidential
      description: "get eeg status via portald"
    - ref: tool://portald/get_versorgung
      mutates: false
      max_sensitivity: confidential
      description: "get versorgung via portald"
    - ref: tool://billingd/list_billing_records
      mutates: false
      max_sensitivity: internal
      description: "list billing records via billingd"
    - ref: tool://billingd/get_billing_record
      mutates: false
      max_sensitivity: internal
      description: "get billing record via billingd"
    - ref: tool://billingd/preview_billing
      mutates: false
      max_sensitivity: internal
      description: "preview billing via billingd"
    - ref: tool://billingd/get_xrechnung
      mutates: false
      max_sensitivity: internal
      description: "get xrechnung via billingd"
    - ref: tool://billingd/check_billing_anomaly
      mutates: false
      max_sensitivity: internal
      description: "check billing anomaly via billingd"
    - ref: tool://billingd/list_vpp_settlements
      mutates: false
      max_sensitivity: internal
      description: "list vpp settlements via billingd"
    - ref: tool://billingd/list_corrections
      mutates: false
      max_sensitivity: internal
      description: "list corrections via billingd"
    - ref: tool://billingd/list_product_categories
      mutates: false
      max_sensitivity: internal
      description: "list product categories via billingd"
    - ref: tool://billingd/validate_tariff_config
      mutates: false
      max_sensitivity: internal
      description: "validate tariff config via billingd"
    - ref: tool://billingd/explain_invoice_position
      mutates: false
      max_sensitivity: internal
      description: "explain invoice position via billingd"
    - ref: tool://billingd/get_billing_summary
      mutates: false
      max_sensitivity: internal
      description: "get billing summary via billingd"
    - ref: tool://einsd/list_plants
      mutates: false
      max_sensitivity: internal
      description: "list plants via einsd"
    - ref: tool://einsd/get_plant
      mutates: false
      max_sensitivity: internal
      description: "get plant via einsd"
    - ref: tool://einsd/list_expiring
      mutates: false
      max_sensitivity: internal
      description: "list expiring via einsd"
    - ref: tool://einsd/list_settlements
      mutates: false
      max_sensitivity: internal
      description: "list settlements via einsd"
    - ref: tool://einsd/lookup_verguetungssatz
      mutates: false
      max_sensitivity: internal
      description: "lookup verguetungssatz via einsd"
    - ref: tool://einsd/list_unsettled_plants
      mutates: false
      max_sensitivity: internal
      description: "list unsettled plants via einsd"
    - ref: tool://einsd/get_epex_monthly_price
      mutates: false
      max_sensitivity: internal
      description: "get epex monthly price via einsd"
    - ref: tool://einsd/lookup_statutory_rate
      mutates: false
      max_sensitivity: internal
      description: "lookup statutory rate via einsd"
    - ref: tool://einsd/get_compliance_status
      mutates: false
      max_sensitivity: internal
      description: "get compliance status via einsd"
    - ref: tool://einsd/list_plants_without_mastr
      mutates: false
      max_sensitivity: internal
      description: "list plants without mastr via einsd"
    - ref: tool://einsd/check_direktvermarktung_compliance
      mutates: false
      max_sensitivity: internal
      description: "check direktvermarktung compliance via einsd"
    - ref: tool://einsd/check_sect44b_quota
      mutates: false
      max_sensitivity: internal
      description: "check sect44b quota via einsd"
    - ref: tool://einsd/get_jahresmarktwert
      mutates: false
      max_sensitivity: internal
      description: "get jahresmarktwert via einsd"
    - ref: tool://einsd/get_aw_reduktionen
      mutates: false
      max_sensitivity: internal
      description: "get aw reduktionen via einsd"
    - ref: tool://einsd/get_settlement_state_history
      mutates: false
      max_sensitivity: internal
      description: "get settlement state history via einsd"
    - ref: tool://einsd/explain_settlement
      mutates: false
      max_sensitivity: internal
      description: "explain settlement via einsd"
    - ref: tool://accountingd/get_balance
      mutates: false
      max_sensitivity: internal
      description: "get balance via accountingd"
    - ref: tool://accountingd/list_ledger
      mutates: false
      max_sensitivity: internal
      description: "list ledger via accountingd"
    - ref: tool://accountingd/list_dunning
      mutates: false
      max_sensitivity: internal
      description: "list dunning via accountingd"
    - ref: tool://accountingd/list_overdue
      mutates: false
      max_sensitivity: internal
      description: "list overdue via accountingd"
    - ref: tool://accountingd/compute_bilanzielle_abgrenzung
      mutates: false
      max_sensitivity: internal
      description: "compute bilanzielle abgrenzung via accountingd"
    - ref: tool://accountingd/suggest_payment_match
      mutates: false
      max_sensitivity: internal
      description: "suggest payment match via accountingd"
