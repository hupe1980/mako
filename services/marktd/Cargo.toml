[package]
name             = "marktd"
version.workspace = true
description      = "Market data daemon for German energy market (MaKo/Marktd)"
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish          = false

[[bin]]
name = "marktd"
path = "src/main.rs"

[lib]
name = "marktd"
path = "src/lib.rs"

[dependencies]
mako-events = { workspace = true }
mako-markt = { path = "../../crates/mako-markt", features = ["makod-client"] }
metering   = { workspace = true, features = ["serde"] }
rubo4e    = { workspace = true, features = ["sqlx"] }

anyhow             = { workspace = true }
thiserror          = { workspace = true }
axum               = { version = "0.8", features = [] }
clap               = { version = "4", features = ["derive", "env"] }
serde              = { version = "1", features = ["derive"] }
serde_json         = { workspace = true }
toml               = { version = "0.8", default-features = false, features = ["parse"] }
time               = { workspace = true }
uuid               = { workspace = true }
tokio              = { version = "1", features = ["full"] }
tokio-util         = { workspace = true }
tracing            = "0.1"
mako-service       = { workspace = true, features = ["otel", "cedar", "oidc", "metrics"] }
rmcp               = { workspace = true }
schemars           = { workspace = true }
secrecy            = "0.10"
subtle             = "2"
hmac               = { version = "0.12", features = ["std"] }
sha2               = "0.10"
jiff               = "0.2"
reqwest            = { workspace = true }
# Bounded-concurrent fan-out delivery (`for_each_concurrent`) so one slow
# subscriber cannot stall a whole batch on the single-threaded worker.
futures            = { version = "0.3", default-features = false, features = ["std"] }
rust_decimal       = { workspace = true }
utoipa             = { version = "5", features = ["axum_extras", "uuid", "time"] }
utoipa-axum        = "0.2"
utoipa-swagger-ui  = { version = "9", features = ["axum"] }
prometheus         = "0.14"
sqlx               = { version = "0.8", features = ["runtime-tokio-rustls", "postgres", "uuid", "time", "json", "rust_decimal"] }

[dev-dependencies]
testcontainers = { workspace = true }
testcontainers-modules = { workspace = true }
# (no `testing` feature: every marktd repository is exercised against a real
# PostgreSQL via testcontainers in tests/*_integration.rs)
tokio      = { version = "1", features = ["full"] }
serde_json = { workspace = true }
