# vertragd.toml — demo configuration
# Contract & Customer Management daemon (LF role).
# DO NOT use in production — auth disabled.

database_url = "postgres://demo:demo-secret@postgres/vertragd"
port         = 9780
tenant       = "9910000000002"   # data-isolation key (any stable string; typically the operator’s BDEW- or DVGW-Codenummer)

# processd base URL — triggers GPKE/GeLi Gas Lieferbeginn/-ende.
processd_url = "http://processd:8580"

# MCP server Bearer token — set a strong secret in production.
# mcp_api_key = ""  # set via VERTRAGD_MCP_API_KEY env var
